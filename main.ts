fuctiontocasom(SelectorAudio{
  constelemento=document.querySelector(SelectorAudio);
  if (elements&&elemento.localname===áudio){
    elemento.play();
  }
  else{
    console.log('elemento não encontrado ou seletor inválido');
    for(let.contador=0;contador<ListaDeTeclas.length
